04.25

1、Can't set headers after they are sent； 
出现这个错误就表示。你在一个已经关闭的连接上又做一些写操作。
无论是res.json()还是 res.send() 
底层代码都是用res.end()结束。
也就是说如果你 res.json()后 又有出现了 res.xxxx()的操作
就会报上面的那个错误

2、vue的计算属性computed是实时监听变量的变化，不可在其他地方进行赋值等修改操作，否则都将在变量值变化后被computed内的操作覆盖修改后的值。

3、nodejs后台从monogDB数据库中查询数据时，必须先实例化一个mongoose.Schema，且数据结构要与数据库保持一致，否则将以mongoose.Schema数据结构为准。

4、数组操作：

删除数据中某个数据，var item=arr.split(index,num,newValue) 直接修改原数组
arr.slice(start,end) 不修改原数组，返回新数组
arr.unshiift(item) 正序插入第一位 push倒序插入最后一位

5、删除数组不连续的多项

方案1.先把对应项重新赋值为undefined，然后使用数组的filter方法筛序掉该项等于undefined的值，获得一个新数组；
方案2.倒着遍历数组，即从数组最后一项向第一项遍历，进行splice操作
方案3.将不需要删除的数据push到新数组保存

